var inputWidth;
var inputHeight;
inputWidth=prompt('enter the width');
inputHeight=prompt('enter the height');
var Width=parseInt(inputWidth);
var Height=parseInt(inputHeight);
var area= Width*Height;
document.write('the arena is:'+area);
